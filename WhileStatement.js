// Inisialisasi variabel counter
let counter = 1;

// Mulai pernyataan while
while (counter <= 10) {
  // Tampilkan nilai counter saat ini
  console.log(counter);

  // Increment nilai counter
  counter++;
}
