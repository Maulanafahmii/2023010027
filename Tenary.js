// Nilai siswa
let nilai = 69;

// Ternary Operator untuk mengecek apakah siswa lulus atau tidak
let status =
  nilai >= 70
    ? "Selamat! Anda lulus."
    : "Maaf, Anda belum lulus. Anda perlu belajar lebih giat lagi.";

// Menampilkan status kelulusan
console.log(status);
