// Deklarasi fungsi untuk mencetak angka dari 1 hingga 10
function printNumbers() {
  // Loop dari 1 hingga 10
  for (let i = 1; i <= 10; i++) {
    // Cetak nilai i saat ini
    console.log(i);
  }
}

// Panggil fungsi untuk mencetak angka
printNumbers();
